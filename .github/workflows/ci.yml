# Run Alexa chat tests on every pull request to main.
name: CI

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  alexa-chat-tests:
    uses: ./.github/workflows/alexa-chat-tests.yml
