# Confirmation Rules

- send email, WhatsApp, delete meeting, delete task, schedule meeting (if setting on) → pendingAction first
- User says yes → execute staged action
- User says cancel → clear pending
- Never execute dangerous tools directly when confirmation is enforced
